# KillSwitch SaaS — environment configuration

NODE_ENV=development
PORT=4000
DATABASE_URL=postgresql://user:password@localhost:5432/killswitch_saas

JWT_SECRET=your_long_random_jwt_secret_here

# Admin creation defaults (only used on first run if no admin exists yet)
ADMIN_DEFAULT_EMAIL=founder@example.com
ADMIN_DEFAULT_PASSWORD=some_very_secure_password

# (Optional) Stripe webhook signing secret — uncomment if using auto-freeze
# STRIPE_WEBHOOK_SECRET=whsec_...
