{
  "name": "killswitch-saas-backend",
  "version": "0.1.0",
  "description": "Backend for KillSwitch SaaS — emergency access shutdown & control plane",
  "main": "dist/server.js",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/server.ts",
    "build": "tsc",
    "start": "node dist/server.js",
    "prisma": "prisma"
  },
  "dependencies": {
    "@prisma/client": "^5.0.0",
    "express": "^4.18.2",
    "zod": "^3.23.2",
    "dotenv": "^16.0.3"
  },
  "devDependencies": {
    "prisma": "^5.0.0",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.2.2"
  }
}
